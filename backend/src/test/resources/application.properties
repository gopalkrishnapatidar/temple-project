# Loaded in addition to src/main/resources/application.yml (different filename).
# Do not add application.yml here; it would hide the main file on the test classpath.
# Test-only signing secrets; production still requires environment secrets.
# Datasource/Flyway URLs come from @IsolatedPostgresIntegrationTest (Testcontainers), not local PostgreSQL.
app.jwt.secret=test-jwt-secret-must-be-at-least-32b!
app.payment.mock.webhook-secret=test-mock-webhook-secret-at-least-32b!
app.notification.kafka.enabled=false
